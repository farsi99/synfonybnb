{% extends "base.html.twig" %}

{% block title %}Inscription sur votre site
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Inscription</h1>
        {{form_start(form)}}
        {{form_widget(form)}}
        <button class="btn btn-success" type="submit">Enregistrer</button>
        {{form_end(form)}}
    </div>
{% endblock %}
